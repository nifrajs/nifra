---
"@nifrajs/testing": patch
---

Map `@nifrajs/testing/certification` in the workspace TypeScript paths, so the subpath resolves from
source like every other first-party entry instead of only through built declarations.
