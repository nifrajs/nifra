---
"@nifrajs/cli": patch
---

Centralize repository verification gates behind stable, ID-addressed plans and expose the same plan through the project MCP server, so default and release verification cannot drift when gates change.
