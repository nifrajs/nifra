---
"@nifrajs/web": patch
"@nifrajs/cli": patch
---

Add an opt-in Vite aggregate-CSS and deferred stylesheet-loading path that prevents lazy-route prefetch from attaching additional stylesheets before hydration.
