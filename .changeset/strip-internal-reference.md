---
"@nifrajs/web-react": patch
---

Remove an internal project reference from the `@nifrajs/web-react/query` module doc comment (it named a
downstream consumer). No API or behavior change — documentation only.
