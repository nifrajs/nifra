---
"@nifrajs/skills": minor
---

Register the Nifra MCP server from the Claude Code plugin manifest, so installing the plugin brings up the live tools the skills point at instead of leaving them to a separate setup step. The launch spec stays unpinned so one installed plugin serves projects on different Nifra versions.
