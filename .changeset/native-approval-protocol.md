---
"@nifrajs/coding-agent": minor
---

Add the native approval protocol flow to `@nifrajs/coding-agent`. Native turns can now emit bounded, ordered approval events and resolve pending approvals through the backend protocol, while retaining compatibility with the existing boolean approval callback.
