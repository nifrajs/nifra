export const meta = { title: "nifra on the edge — about" }

export default function About() {
  return (
    <div>
      <h1 id="page">About</h1>
      <p id="about">
        File-routed SSR + hydration on Cloudflare Workers — no filesystem at runtime.
      </p>
    </div>
  )
}
