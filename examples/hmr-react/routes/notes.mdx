export const meta = { title: "Notes (MDX, dev)" }

# Notes — MDX in dev

This page is **MDX**, compiled live by `@mdx-js/rollup` in nifra's Vite dev server — the same hot dev
loop as the `.tsx` routes. Edit this Markdown and Vite hot-updates the page.

nifra's MDX is framework-agnostic: this is the React dev build.
