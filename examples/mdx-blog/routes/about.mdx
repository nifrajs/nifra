export const meta = { title: "About — nifra MDX blog" }

# About this blog

This page is **MDX**, compiled to a Solid component by `solidMdxBunPlugin` — Markdown prose,
server-rendered and hydrated, with `export const meta` driving the `<title>`.

The exact same `.mdx` authoring works on React, Preact, Vue, and Svelte too: nifra is
**one contract, five renderers**.
