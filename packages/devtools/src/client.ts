/**
 * Re-export the client script for consumers who only need the browser overlay.
 *
 * Usage:
 * ```ts
 * import { devtoolsClientScript } from "@nifrajs/devtools/client"
 * ```
 */
export { devtoolsClientScript } from "./index.ts"
