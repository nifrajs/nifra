# @nifrajs/ts-plugin - LLM contract card

<!-- GENERATED by scripts/gen-llm-cards.ts. Do not edit by hand - run `bun run gen:cards`. -->

TypeScript language-service plugin for nifra - go-to-definition from a route-path string literal to the routes/ file that serves it.

> A tight contract card for AI agents: the exports you actually call + the footguns. For the full
> reference see [`api-reference.md`](../../api-reference.md) (every export + signature) and
> [`llms-full.txt`](../../llms-full.txt) (the prose guides). One cheap read instead of the whole corpus.

## Public entrypoints

`@nifrajs/ts-plugin` · `@nifrajs/ts-plugin/resolve`

## Key exports

- **default** _(function)_ - `default: (modules: { typescript: typeof ts; }) => ts.server.PluginModule` · from `@nifrajs/ts-plugin`
- **findRoutePathLiteral** _(function)_ - `findRoutePathLiteral: (tsm: typeof ts, source: ts.SourceFile, position: number) => { text: string; span: ts.TextSpan; } | undefined` · from `@nifrajs/ts-plugin`
- **findRoutesDir** _(function)_ - `findRoutesDir: (fromFile: string, exists?: (p: string) => boolean) => string | undefined` · from `@nifrajs/ts-plugin`
- **resolveRouteFile** _(function)_ - `resolveRouteFile: (path: string, routes: readonly RouteLocation[]) => string | undefined` · from `@nifrajs/ts-plugin/resolve`
- **RouteLocation** _(interface)_ - `interface RouteLocation` · from `@nifrajs/ts-plugin/resolve`

## Footguns

- No package-specific footguns beyond the framework conventions. See [`AGENTS.md`](../../AGENTS.md) and [`llms-full.txt`](../../llms-full.txt) for the full contract.
