# @nifrajs/agent-telemetry — LLM contract card

<!-- GENERATED by scripts/gen-llm-cards.ts. Do not edit by hand — run `bun run gen:cards`. -->

Agent telemetry plugin — child spans for AI tool calls on /_nifra/tool/* and MCP endpoints.

> A tight contract card for AI agents: the exports you actually call + the footguns. For the full
> reference see [`api-reference.md`](../../api-reference.md) (every export + signature) and
> [`llms-full.txt`](../../llms-full.txt) (the prose guides). One cheap read instead of the whole corpus.

## Key exports

- **agentTelemetry** _(function)_ — `agentTelemetry: (options: AgentTelemetryOptions) => { name: string; beforeHandle(context: HookContext): undefined; onError(error: unknown, …`
- **consoleAgentExporter** _(function)_ — `consoleAgentExporter: (log?: (line: string) => void) => AgentSpanExporter`
- **AgentTelemetryOptions** _(interface)_ — `interface AgentTelemetryOptions`
- **AgentSpan** _(type)_ — `type AgentSpan = NifraSpan`
- **AgentSpanExporter** _(type)_ — `type AgentSpanExporter = SpanExporter`

## Footguns

- No package-specific footguns beyond the framework conventions. See [`AGENTS.md`](../../AGENTS.md) and [`llms-full.txt`](../../llms-full.txt) for the full contract.
