# @nifrajs/storage — LLM contract card

<!-- GENERATED by scripts/gen-llm-cards.ts. Do not edit by hand — run `bun run gen:cards`. -->

Blob storage for nifra — one StorageAdapter interface with memory, local-filesystem, and Cloudflare R2 adapters (key-traversal-safe). The persistence half of @nifrajs/uploads. Dependency-free; bring your own adapter for S3/GCS.

> A tight contract card for AI agents: the exports you actually call + the footguns. For the full
> reference see [`api-reference.md`](../../api-reference.md) (every export + signature) and
> [`llms-full.txt`](../../llms-full.txt) (the prose guides). One cheap read instead of the whole corpus.

## Key exports

- **assertSafeKey** _(function)_ — `assertSafeKey: (key: string) => void`
- **toBytes** _(function)_ — `toBytes: (data: StorageData) => Uint8Array`
- **FileStorage** _(class)_ — `class FileStorage`
- **MemoryStorage** _(class)_ — `class MemoryStorage`
- **R2Storage** _(class)_ — `class R2Storage`
- **StorageKeyError** _(class)_ — `class StorageKeyError`
- **ListOptions** _(interface)_ — `interface ListOptions`
- **PutOptions** _(interface)_ — `interface PutOptions`
- **R2BucketLike** _(interface)_ — `interface R2BucketLike`
- **R2ObjectLike** _(interface)_ — `interface R2ObjectLike`
- **StorageAdapter** _(interface)_ — `interface StorageAdapter`
- **StorageObject** _(interface)_ — `interface StorageObject`
- **StorageData** _(type)_ — `type StorageData = Uint8Array | ArrayBuffer | string`

## Footguns

- No package-specific footguns beyond the framework conventions. See [`AGENTS.md`](../../AGENTS.md) and [`llms-full.txt`](../../llms-full.txt) for the full contract.
